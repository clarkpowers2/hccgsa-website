export default function StatCard({ number, label, sub, light = false }: { number: string; label: string; sub?: string; light?: boolean }) { return <div className={`${light ? 'bg-white/5' : 'bg-haven-navy'} px-4 py-7 text-center`}><div className="text-3xl font-bold text-haven-gold">{number}</div><div className="mt-1 text-sm font-bold text-white">{label}</div>{sub && <div className="mt-1 text-xs text-white/50">{sub}</div>}</div> }
