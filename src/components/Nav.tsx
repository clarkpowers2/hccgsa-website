import { useState } from 'react'
import { Link, useLocation } from 'react-router-dom'
import SealBadge from './SealBadge'
const links = [['/', 'Home'], ['/about', 'About'], ['/portfolio', 'Portfolio'], ['/haven', 'Haven Memory OS'], ['/research', 'Research'], ['/patents', 'Patents'], ['/contact', 'Contact']] as const
export default function Nav() { const [open, setOpen] = useState(false); const { pathname } = useLocation(); return <header className="sticky top-0 z-50 border-b-2 border-haven-gold bg-haven-navy shadow-lg"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-3 md:px-8"><Link to="/" onClick={() => setOpen(false)} className="flex items-center gap-3"><SealBadge size="sm" /><span><span className="block text-sm font-bold tracking-wide text-haven-gold">HCCGSA LLC</span><span className="block text-[10px] uppercase tracking-[0.18em] text-white/60">ARCHITEK N ADVOCACY</span></span></Link><div className="hidden items-center gap-5 lg:flex">{links.map(([href, label]) => <Link key={href} to={href} className={`text-sm font-semibold transition ${pathname === href ? 'text-haven-gold' : 'text-white/80 hover:text-haven-gold'}`}>{label}</Link>)}</div><div className="flex items-center gap-2"><a href="https://haven-mos.org" target="_blank" rel="noopener noreferrer" className="hidden bg-haven-gold px-4 py-2 text-sm font-bold text-haven-navy transition hover:bg-yellow-300 sm:block">Visit Haven →</a><button type="button" aria-label="Toggle navigation" aria-expanded={open} onClick={() => setOpen(!open)} className="rounded border border-white/20 px-3 py-2 text-xl text-white lg:hidden">{open ? '×' : '☰'}</button></div></nav>{open && <div className="border-t border-white/10 bg-haven-navy px-5 pb-4 lg:hidden">{links.map(([href, label]) => <Link key={href} to={href} onClick={() => setOpen(false)} className={`block border-b border-white/10 py-3 text-sm font-semibold ${pathname === href ? 'text-haven-gold' : 'text-white'}`}>{label}</Link>)}<a href="https://haven-mos.org" target="_blank" rel="noopener noreferrer" className="mt-4 block bg-haven-gold px-4 py-3 text-center text-sm font-bold text-haven-navy">Visit Haven →</a></div>}</header> }
